# Uniform, conflict-free dependency set for the code-runner timeout calibration.
# Deliberately ONE set for every candidate script (rather than each script's own pins) so the
# measured number is a *runtime*, not dependency-install variance — and so the known model-family
# environment conflict is structurally avoided: NO catboost (its co-load-with-LightGBM/XGBoost stall
# and its catboost==1.2.5-vs-numpy-2.x ABI break would register as a hang/crash, not a runtime).
# Recent, coherent versions matching what the recent agent runs pinned.
pandas==2.2.2
numpy==2.1.0
scikit-learn==1.5.1
lightgbm==4.5.0
xgboost==2.1.1
