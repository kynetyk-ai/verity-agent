"""Domains — per-domain schema / tools / gate bindings that configure the kernel.

The v1 validation target is **feature engineering** (spec §12, :mod:`.feature_engineering`), which
lands in ROADMAP Phase 4 and defines the MVP. The deterministic ``fake`` domain (§8 contract probe)
and the minimal ``code`` domain (Phase 2.4) precede it; import domains from their modules directly.
"""
