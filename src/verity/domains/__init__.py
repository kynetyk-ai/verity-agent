"""Domains — per-domain schema / tools / gate bindings that configure the kernel.

The v1 validation target is feature engineering (spec §12), which lands in ROADMAP
Phase 4 and defines the MVP. Empty until then.
"""
